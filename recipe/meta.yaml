{% set version = "1.4.0" %}

package:
  name: ctd
  version: {{ version }}

source:
  url: https://github.com/pyoceans/python-ctd/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 24a59dde5a540bda33b98c519d7159b6cc99611041cc6b7ae4dbd5bf71c060ec

build:
  number: 0
  noarch: python
  script:
    - export SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}
    - {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3
    - pip
    - setuptools-scm
  run:
    - python >=3
    - gsw >=3.3.0
    - matplotlib-base
    - numpy
    - pandas-flavor
    - pandas >=0.24.0
    - scipy


test:
  imports:
    - ctd

about:
  home: https://github.com/pyoceans/python-ctd
  license: BSD-3-Clause
  license_file: LICENSE.txt
  summary: Tools to load hydrographic data formats as pandas DataFrames

extra:
  recipe-maintainers:
    - ocefpaf
